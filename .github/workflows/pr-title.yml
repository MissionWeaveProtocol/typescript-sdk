name: Conventional PR title

on:
  pull_request:
    types: [opened, edited, reopened, synchronize, ready_for_review]

permissions:
  contents: read
  pull-requests: read

jobs:
  conventional-title:
    uses: missionweaveprotocol/.github/.github/workflows/pr-title.yml@main
